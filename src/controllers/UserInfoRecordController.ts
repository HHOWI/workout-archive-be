import { Request, Response } from "express";
import { UserInfoRecordService } from "../services/UserInfoRecordService";

export class UserInfoRecordController {
  private userInfoRecordService: UserInfoRecordService;

  constructor() {
    this.userInfoRecordService = new UserInfoRecordController();
  }

  // 닉네임으로 UserInfoRecord 조회
  getUserInfoByNickname = 
  
}
